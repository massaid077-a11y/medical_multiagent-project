# Variables d'environnement (optionnelles) pour LangGraph Studio
# et pour une eventuelle cle d'API LLM si un modele externe est branche.
#
# Exemple (non utilise par defaut, le projet fonctionne sans cle) :
# OPENAI_API_KEY=
